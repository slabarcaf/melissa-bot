# Handoff: Melissa/Sydney bot

Last updated: 2026-09-01. Pick-up notes.

## What this is

Multi-user Telegram assistant "Sydney" (repo `melissa-bot`): tasks, finance/debts, calendar, Gmail
triage, networking follow-ups, morning/evening briefs. Node, long-polling, OpenAI tool-calling into
MCP child processes.

This session opened a multi-phase push to turn Sydney into something other people can use. The
roadmap is `PLAN-2026-09-01-producto.md` in this directory. **Phases 0 and 1 are done and live.**

## Status (lead here)

- **Service:** `melissa-bot.service` on `$VM_HOST`, `active`, non-root user `melissa`,
  `/opt/melissa/whatsapp-bot`.
- **Deployed file:** md5 matches repo HEAD. Verified after every deploy this session.
- **Branch: `main`.** The long-running `pause-morning-brief` divergence is **resolved and the branch
  is deleted**. Three prior handoffs carried this; it is done. Cloning the default branch now gives
  exactly what runs in production (verified by a fresh clone + md5 comparison).
- **`SELFCHECK=1`: ALL PASS** (6/6) after the final deploy.
- **Tests:** still none in the repo. Validation is `node --check`, `SELFCHECK=1`, and extracting a
  function from the deployed file to exercise it directly (see cheat-sheet).
- Networking brief section: still PAUSED, unchanged from 2026-08-26.

## Shipped this session

**1. Phase 0 — the safety net**

The Task Dashboard's bot-auth layer (`getBotUserIfAuthorized`, the `DELETE` route,
`deleteDbTaskForUser`) had been deployed to Vercel straight from a working copy and **never
committed**. GitHub was behind production and the code existed only on Santiago's Mac.

- Committed and pushed. The repo also moved: it was under the `santiagolabarca` GitHub account,
  which is not authenticated on this machine. Now at **`github.com/slabarcaf/task-dashboard`**
  (private), same account as `melissa-bot`. The old remote is kept as
  `old-origin-santiagolabarca`.
- Documented `OPENCLAW_API_SECRET` in `.env.example` — it is the same value the bot calls
  `task_api_secret`, and it was invisible from the repo.
- `CLAUDE.md` now documents the Task Dashboard: repo, URL, stack, and the fact that its bearer token
  **resolves every bot user to one owner account**.

**2. Phase 0 — branch resolution**

`pause-morning-brief` merged into `main` (content verified identical), deploy script defaulted to
`main` both in `ops/` and on the VM (`/root/deploy-melissa.sh`, backup kept), stale branch deleted
locally and on origin, `CLAUDE.md` invariant rewritten. Deployed from `main` and confirmed the
production md5 did not change.

**3. Phase 1 — Telegram actually renders now** (`f45f983` and the commit before it)

Root cause of the "ugly briefs": **`sendMessage` never passed `parse_mode`.** `parse_mode` appeared
zero times in the file. Every `*bold*` the bot wrote — the whole brief, every onboarding message —
reached users as literal asterisks.

- Messages now render as **HTML**, not Markdown. HTML needs only `& < >` escaped; Markdown breaks on
  any stray `*`, `_` or `[` inside a user's own task title and makes Telegram reject the message.
- `toTelegramHtml()` escapes everything first, then reopens only the tags the bot emits, so user
  content can never introduce markup.
- The italic rule is deliberately narrow: `_` only pairs at word edges, so `America/Los_Angeles` and
  `America/New_York` in one message are **not** turned into italics. There is a regression test for
  exactly this.
- **Plain-text fallback:** if Telegram still rejects a message, it is resent without `parse_mode`
  rather than lost.
- Chunking splits on line boundaries, never mid-tag (the old code sliced blindly at 4000 chars).
- `/users` and `/invite` use `<code>` spans instead of backticks, which never rendered.

**4. Phase 1 — task line format**

Santiago chose bullet + bold, date in parentheses, no dash:

```
✅ TAREAS          (bold)
Finanzas           (bold)
• Pagar la tarjeta (3-Mar)
• 🔴 Renovar el seguro (5-Mar)
```

Two places, one of them **not in this repo**: the `FORMAT RULES` templates in `melissa.js`, and
`fmtDate` in `tasks-mcp.js` on the VM, which produced `"Lun 3 mar"` and now produces `"3-Mar"`
(with the year appended only when it is not the current year: `15-Ene-2027`). Patched on the VM with
an assert-before-replace script, backups at `*.bak-20260902-035325`, mirrored to
`/root/.openclaw/skills/`.

**5. Phase 1 — the voice-path defect from the last handoff is FIXED**

Neither `tgRequest` nor `downloadFile` had a timeout, and Node's `https` has no default, so a stalled
Telegram call wedged that chat's FIFO queue until a restart.

- `tgRequest` takes a per-call deadline, default 20s. `getUpdates` passes 45s so long-polling still
  works — this is the trap to remember if you touch it.
- `downloadFile` has a 30s deadline, checks the status code, and cleans up the partial temp file.
- The Whisper call gets an `AbortSignal` (60s); it had no per-call deadline either.

`VOICE_PROMPT_ES` is still unused. The blocker named in the 2026-08-26 handoff — "safe to re-enable
once those have timeouts" — **is now cleared**, so rewiring it is a clean next step.

**6. Task API moved to a Vercel project under Santiago's own account**

The dashboard used to be deployed from a working copy via `vercel --prod` into a Vercel *team*
project, so production depended on one laptop. A fresh project was created under the Berkeley Vercel
account, connected to `github.com/slabarcaf/task-dashboard`, and the bot cut over to it.

- `task_api_base` in the VM's `config.json` is now **`https://task-dashboard-c7q2.vercel.app`**
  (backup `config.json.bak-20260902044729`; the old value was `…-nine-ashen…`).
- The stale CLI link is archived as `.vercel-OLD-PROJECT-backup` in `~/demo/task-dashboard` so a
  stray `vercel --prod` cannot publish to the retired project. Deploys now come from GitHub.
- **Both projects share the same Neon database**, which is why the cutover needed no data migration
  and why the old project still works as a fallback.

Two traps this hit, worth knowing before touching Vercel again:

- **Environment variables only take effect on a rebuild.** `NEXT_PUBLIC_*` are inlined at build
  time; the server-side ones need a redeploy too. A first attempt (`…-six-snowy…`) built without
  them and is permanently frozen in that broken state — it still returns 401 to the bot today.
- **Google Sign-In breaks on a new URL** until that exact origin is added to the *Authorized
  JavaScript origins* of the **web** OAuth client `…-p82fvab9n0fkdou4nevr0na7k8q9jbs3`. The other
  client in that Google project is the bot's, and must not be touched.

## Verified, not assumed

- 17 rendering cases pass, then re-run against the **deployed** file rather than the local one.
- Live MCP call returns real tasks with the new dates: `Sacar ikon pass de correo — 7-Sep [#526]`.
- `fmtDate` exercised on the VM across current year, next year, and prior year.
- Fresh `git clone` of the default branch produces a `melissa.js` whose md5 equals production's.
- After the Vercel cutover: `SELFCHECK` ALL PASS; the live MCP returns real tasks through the new
  project; and a full write cycle against it (create → read back → delete → confirm gone) left the
  table back at its original 517 rows.

## Next steps (prioritized)

1. **Phase 2 — documentation.** `ONBOARDING.md` (the 11-state flow exists only in code, and is the
   single source the future web onboarding must mirror) and `MODULES.md`. Also fix
   `~/Documents/CLAUDE/openclaw/openclaw-project-summary.md`, which is still wrong about the service
   name, path, and model.
2. **Rewire `VOICE_PROMPT_ES`** — its blocker is gone.
3. **Phase 3 — real accounts.** `users.telegram_chat_id`, `tasks.is_priority`, per-user resolution in
   `getBotUserIfAuthorized`, migrate the `[uid:]`-tagged tasks and `priority.json`, `/link` flow.
   This is the riskiest phase: it touches production data in use.
4. **Phase 4 — web/PWA**, then **Phase 5 — per-user Google OAuth**.
5. Carried over: rotate `TASK_API_SECRET`; make journald persistent; duplicate-task guard on
   `add_task`; decommission the old root `whatsapp-bot.service`.

## Open questions

- Vercel still builds `task-dashboard` from the **old** GitHub account's project link
  (`.vercel/project.json`). Deploys today go through `vercel --prod` from the working copy. Worth
  deciding whether to relink the Vercel project to `slabarcaf/task-dashboard` and turn on git
  deploys, so production stops depending on one laptop.
- Three category vocabularies still disagree (`TIPO_OPTIONS`, `ONBOARDING_SUGGESTED_TIPOS`,
  `categories.json`), and `tipo` is unconstrained TEXT. Pick one before the web UI exposes it.
- `statusNextStep` is computed in UTC on Vercel while the bot computes "today" in the user's zone —
  off by one near midnight.
- Carried from the last handoff: does "pause networking" also mean mute on demand? Still unanswered.

## Run / test / deploy (cheat-sheet)

```bash
ssh -i ~/.ssh/id_ed25519 opc@$VM_HOST
ssh -i ~/.ssh/id_ed25519 opc@$VM_HOST 'sudo /root/deploy-melissa.sh'   # defaults to main now
ssh -i ~/.ssh/id_ed25519 opc@$VM_HOST 'sudo md5sum /opt/melissa/whatsapp-bot/whatsapp-bot.js'
md5 -q melissa.js                                                            # must match the line above
```

Exercise the renderer against what is actually deployed, without messaging anyone:

```bash
ssh -i ~/.ssh/id_ed25519 opc@$VM_HOST 'sudo node -e "
const src=require(\"fs\").readFileSync(\"/opt/melissa/whatsapp-bot/whatsapp-bot.js\",\"utf8\");
const s=src.indexOf(\"const TG_ALLOWED_TAGS\"), e=src.indexOf(\"async function sendMessage\");
const m=new Function(src.slice(s,e)+\"; return {toTelegramHtml};\")();
console.log(m.toTelegramHtml(\"✅ *TAREAS*\n• Tarea <x> & y (3-Mar)\"));
"'
```

Backups from this session (rollback targets):

```
/opt/melissa/.openclaw/skills/tasks-mcp.js.bak-20260902-035325
/root/.openclaw/skills/tasks-mcp.js.bak-20260902-035325
/root/deploy-melissa.sh.bak-*
```

Journald is still not persistent on this VM. Capture what you need while debugging.
